import { expect } from 'chai';
import { describe, it } from 'mocha';
import halfSample from '../halfSample';

describe('halfSample', () => {
    it('tests', () => {
        console.warn('* halfSample test running');
        expect(1).to.equal(1);
    });
});
